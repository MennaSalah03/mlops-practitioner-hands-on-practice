[project]
name = "project-1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mennasalah", email = "menna.skh2003@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "joblib>=1.5.3",
    "litestar>=2.24.0",
    "pandas>=3.0.5",
    "scikit-learn>=1.9.0",
]

[project.scripts]
project-1 = "project_1:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
